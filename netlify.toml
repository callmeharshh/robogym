# RoboGym — static site, no build step.
[build]
  publish = "."
  command = ""

# Pretty + safe defaults
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
